add_subdirectory( feel )

# if ( ${FEELPP_CORE_CMAKE_PREFIX}_ENABLE_APPLICATIONS )
#   add_subdirectory( applications )
# endif()
# if ( ${FEELPP_CORE_CMAKE_PREFIX}_ENABLE_TEST_SUITE )
#   add_subdirectory( tests )
# endif()
